from django.urls import path
# from .views import api_services, api_service, api_show_appointment, api_technician
# technician ? 
#needs to be updated with view name
urlpatterns = [
    # path("services/", api_services, name="api_services")
    # list view
    # path("services/<int:pk>", api_service, name="api_service") 
    # detail view 
    # path("services/history/", api_show_appointment, name="api_appointment")
    # service history 
    # path("technician/", api_technician, name="api_technician")
    # create tech
]
